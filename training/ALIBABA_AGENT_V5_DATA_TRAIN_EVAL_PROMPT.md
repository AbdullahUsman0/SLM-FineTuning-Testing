# Copy this prompt to the Alibaba coding agent

You are my autonomous research engineering agent for a forecasting-assistant small language model. You may use my already-authorized GitHub, Google Drive, and Google Colab sessions and interact with the browser to complete the work, including selecting a Colab GPU, mounting Drive, running training, resuming checkpoints, and downloading reports. If authentication or 2FA requires me, pause only for that step and resume immediately afterward. Never print, commit, or share passwords, cookies, API keys, or private source data. Use Colab Secrets for `OPENAI_API_KEY`; never put it in a notebook output, Git, or a persistent `.env`. Ask me before spending a substantial new amount on paid API calls or upgrading Colab. My access to this agent ends soon, so start durable, resumable work first and continue autonomously.

## Objective and ground truth

Repository: `https://github.com/AbdullahUsman0/SLM-FineTuning-Testing`. Dependency: `https://github.com/int-abd-5/fpy`, historically pinned to `04d52c015d1e3ecdefe92b87116f209361509b4b`. Current reviewed repository commit is `7cc08f39272038f4c84ef4d41e64f436ce778742`; fetch latest, inspect the actual state, and record the exact commit used. Read `RESEARCH_CHECKPOINT_2026-09-19.md`, `HANDOFF_AUDIT_2026-09-17.md`, `training/COLAB_2B_V3_REPRO.md`, the corpus README/manifest files, `training/train_lora.py`, `local_slm_lab/component_eval.py`, the Pydantic schema in `fpy`, and all existing per-case reports if available. Do not assume the handoff's missing v4 adapter or paraphrase cache exists.

Current verified 2B run: `Qwen/Qwen3.5-2B` with rank-16 / alpha-32 LoRA, 2 epochs, LR 5e-5, effective batch 16, max 3072 without truncation on Colab Tesla T4, v3 extraction-only SFT 865 train / 184 validation. Adapter SHA-256 `538da1675b195ce1ca119e7a8cbc84bcf21d1b2f6928412d0bea8587fa2eaf13`; existing Drive run `MyDrive/FYP-model-runs/qwen35-2b-lora-v3-repro-20260918/`. On the same 48 v3 validation scenarios, exact non-intent slot F1 is 0.0714 for stock 2B, 0.0938 for this LoRA, and 0.3409 for the OpenAI `gpt-5-mini-2025-08-07` production client, with 9, 12, and 75 correct of 234 gold non-intent fields respectively. The LoRA often emits only intent and misses multi-slot details and correction turns. OpenAI improves extraction recall but has a 0.1795 forbidden-slot inference rate and only 0.6410 question-contract accuracy, versus zero and 0.9231 for LoRA. The raw v3 three-way reports are in `research-checkpoints/2026-09-20-v3-validation/`; `scripts/compare-validation-reports.py` reproduces the paired comparison. These are validation results, with different runtime/prompt implementations, not an untouched final test or a model-only effect. Old OpenAI `0.4068` is from a separate historical cohort. Your task is a new, better-grounded corpus and a fair, recorded v5 evaluation.

## Deliverable order: preserve progress before the agent expires

1. Make a new branch for this experiment; keep existing runs intact. Inventory Git, Drive, Colab runtime, model artifacts, reports, available disk/RAM/VRAM, and package versions. Write `v5-plan-and-inventory.md` with exact source links, commit IDs, hashes, expected GPU time, and clearly marked missing evidence. If the repo checkout lacks the checkpoint files, use the content in this prompt and do not fabricate them.
2. Freeze a new, previously unseen final test set and a development validation set **before training or choosing a model**. Keep the final test labels sealed from prompts, training, hyperparameter decisions, and per-case inspection until the model and thresholds are frozen. Existing v3 test cases have been used for exploratory model selection and are not a fresh final test.
3. Build and validate the v5 corpus, run a small pilot, then launch the full Colab training early. Save checkpoints and logs to a new unique Drive directory after each epoch; resume automatically if Colab disconnects. Do not overwrite the v3 run. If time runs out, leave the dataset, manifests, executable notebook, Drive checkpoint path, and exact resume instructions.
4. Run paired evaluations, error analysis, and write a research report with measured results and limitations. Commit code, validators, manifests, and safe summaries to your branch. Push that branch if the repository account permits, and give me its URL/commit. Keep large weights, raw sensitive data, and secrets out of Git.

## Build the v5 dataset

Use the `fpy` `ExtractorResult`, `DialogueState`, question-generation contract, and canonical slot IDs/status values as the single source of truth. This is a structured forecasting-request assistant: the model must identify the intent, extract **all** stated slots with exact canonical values and evidence, distinguish explicit versus inferred/unknown information, avoid inventing file contents or sensitive facts, update prior state correctly, and ask only necessary next questions. Preserve separate extraction and question-generation examples; weight extraction more heavily because extraction recall is the current bottleneck. Keep historical v1–v3 data as an explicitly labeled source subset after auditing for leakage and labeling mistakes. Treat v4 paraphrases as untrusted until each fact is checked.

Target **1,500–2,500 distinct source scenarios** and roughly **6,000–10,000 accepted SFT examples** after augmentation and deduplication; adjust to available review and Colab capacity and report actual counts. Quality is more important than reaching a number. Cover at least 10 forecasting contexts (business sales/demand, inventory, finance/budget, staffing, energy, weather, transport, health operations using synthetic data, education, web traffic, and supply chain). Include varied lengths, formal/informal English, Roman Urdu/Urdu only when reviewers can verify labels, file references, dates/ranges, units, missing fields, contradictions, corrections, negation, ambiguous requests, unsupported intents, privacy boundaries, and multi-turn state updates. Synthetic examples must use fictional names and data; do not import private records or copyrighted corpora without documented rights.

Design the **scenario-grouped training mixture** so approximately 45–55% of extraction examples require 3–8 non-intent slot updates, 15–20% require 9+ where the schema permits, 10–15% test corrections/state conflicts, 10–15% are short selected-slot answers, and 5–10% are true non-answer/unsupported or abstention cases. These are starting targets, not exact quotas; inspect actual update-count and slot-frequency histograms and avoid the previous roughly 75%-style zero/one-update bias. Ensure every required slot appears enough times in both simple and complex contexts, especially `file_format`, `target_description`, `problem_statement`, `source_mode`, and `source_reference`. Include positive and negative examples for each slot and never mark absence as an explicit value.

Generate from reviewed scenario facts and schema-aware templates, then optionally paraphrase. Each record must retain `scenario_id`, `source_fact_id`, `turn_id`, parent/paraphrase ID, origin, language, template family, expected structured JSON, evidence spans from the actual user turn, and reviewer status. Validate parsed JSON against the Pydantic schema; check that every evidence span exists in the input; audit whether each paraphrase preserves every labeled fact; reject changed numbers, dates, polarity, units, filenames, targets, unsupported inferences, or impossible state transitions. Use exact/normalized dedup plus near-duplicate review. Human-review all final test scenarios, all uncertain examples, and stratified samples of each training category; record acceptance/rejection counts and reasons. Produce per-slot coverage, update-count distribution, class balance, length distribution, data provenance, and a leakage report.

Split by source scenario and paraphrase family, never by individual utterance. Keep all turns, variants, and question examples derived from a scenario in one split. Aim ~70% train / 15% validation / 15% final test, but report the final counts and ensure the test includes distinct templates/domains to measure generalization. Run exact and fuzzy overlap checks across train/validation/test. Keep a small development smoke subset outside the final test. Write UTF-8 JSONL, a schema/version document, generation code, review decisions, and SHA-256 manifests. Do not silently regenerate a frozen corpus: use a new version/run ID for any change.

## Train on Colab yourself

Use my active Colab account through the browser; select a GPU runtime and mount `MyDrive`. Prefer an L4 if available; a T4 already completed the 2B setup. Verify CUDA, VRAM, free Drive space, repository and dependency commits, corpus hashes, and installed versions in the first notebook cells. The laptop has no CUDA; its Intel NPU is not the training target. Download `Qwen/Qwen3.5-2B` with a pinned revision and record its license and hash/revision. Train adapters against the Hugging Face base, not the quantized GGUF. Use completion-only loss for the exact structured output and the same chat template at training/inference. Start with the proven rank 16, alpha 32, effective batch 16, seed 42, gradient checkpointing, and a measured max sequence length that causes **zero truncation**. Record LoRA target modules, optimizer, warmup, scheduler, dtype, gradient clipping, and all generation parameters. Pilot on a small training subset and the development validation set to catch formatting, leakage, JSON, and prompt mismatch before full training.

Run a modest predeclared sweep only if compute allows: LR 2e-5 versus 5e-5 and 2 versus 3 epochs or early stopping. Do not tune on final test. Save an epoch checkpoint, best adapter, tokenizer/config, metrics, `run-manifest.json`, `pip freeze`, Colab GPU details, stdout/stderr logs, input/output SHA-256, and a resume marker in a **new** Drive folder such as `MyDrive/FYP-model-runs/qwen35-2b-lora-v5-<UTC timestamp>/`. Recheck hashes after upload. Make the notebook runnable cell-by-cell after a runtime reset, with imports and setup in each independently rerunnable stage; never depend on forgotten Colab variables. On interruption, inspect the last complete checkpoint and resume without changing data or optimizer settings. Do not declare success from low eval loss or high token accuracy alone.

## Compare fairly with stock 2B and OpenAI

Run three candidates on the **same frozen v5 validation scenarios** using the same schema/scorer revision and record all per-case outputs: (A) stock `Qwen/Qwen3.5-2B` in full precision on the same Colab GPU, (B) the selected v5 2B LoRA on that base/runtime, and (C) the project's `OpenAIProductionProvider`, initially `gpt-5-mini-2025-08-07`, which successfully completed the v3 same-case benchmark on 2026-09-20. The official model page marks that snapshot deprecated; if it becomes unavailable, use a documented accessible replacement, record its exact ID, and clearly distinguish it from the old result. Use the pinned `fpy` client, same scenario file, scorer commit, output schema, and equivalent task instructions where provider implementations permit; disclose prompt or decoding differences rather than claiming a perfectly controlled model-only experiment. Log API request count, token usage, billed cost/rate if available, and errors without logging secrets. Use a freshly rotated `OPENAI_API_KEY` from Colab Secrets and set `OPENAI_MODEL` in the process environment; `scripts/evaluate-openai-resumable.py --cases <validation JSONL> --output <report JSON>` accepts environment variables and saves after each scenario. Install the project's and `fpy` dependencies first. If the OpenAI key or model access is unavailable, leave an executable comparison cell and mark its outcome **not measured**; never substitute a v3 score as a v5 score.

Evaluate exact slot micro precision/recall/F1 both including and excluding intent, per-slot precision/recall/F1, scenario-level all-required-slots accuracy, correction accuracy, intent accuracy, invalid JSON, provider success, hallucinated/forbidden slot inference, empty-update collapse, question contract, and per-call latency. Audit raw outputs because provider recovery/guardrails can hide model formatting failures. Primary model-selection metric is **non-intent exact slot F1**, with hard requirements of no increase in forbidden inference and no material JSON/correction regression. Report confidence intervals using paired scenario-cluster bootstrap with fixed seed and at least 10,000 resamples. For speed, cost, and energy, compare only matched hardware/runtime and clearly label separate API and laptop measurements. Include the current v3 validation results in a *historical context* table, not as a direct v5 test comparison.

After choosing the model, prompts, thresholds, and checkpoints on validation, freeze the decision in a dated manifest, then run each candidate **once** on the sealed v5 final test. Produce the same per-case reports and paired bootstrap intervals. If results disappoint, report them honestly; create a later v6 test for further changes instead of iterating on this final test. Do not claim production readiness without materially better non-intent extraction across incomplete/correction scenarios and a tested local deployment path. If quantizing/converting the winner for the laptop, compare full-precision and converted outputs on identical cases and report any degradation before promotion.

## Final handoff to me

Return: branch/commit URL; Drive run folder; exact corpus and adapter hashes; counts/split/leakage/review summary; Colab GPU/hours and package/model revisions; training curve and best-checkpoint criterion; validation and sealed-test comparison tables for all candidates; raw report paths; paired confidence intervals; cost/latency with hardware; top error categories with examples; a paper-ready methods/results paragraph that states limitations; and clear reproduction/resume commands. State explicitly what was completed versus planned. Keep the v3 adapter and all existing reports unchanged.
