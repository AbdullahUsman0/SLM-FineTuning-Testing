# V5 implementation audit — 2026-09-21

## Repository and verification

- Branch `experiment/v5-grounded-20260919` was fetched from `origin` at `634b4684a42e03a8d6de17b04bf914e1fb210b54`; parent v5 commit `b1bea52` and fork point `7cc08f3` match the handoff.
- The committed tree was clean before this audit. `python -B -m unittest discover -s tests -t tests` passed 155 tests. Full local corpus verification passed without parsing sealed labels: 1,309 development scenarios, 5,383 checked development model inputs, zero exact or normalized full-input duplicates. The sealed local final files matched the committed manifest by byte size and SHA-256.
- The v5 statistics support the handoff counts: 1,540 scenarios, 6,336 SFT examples, 4,796 extraction examples, 1,540 questions, 11 domains, and the declared dense/correction/short/abstention mixture. All required schema slots have positive examples; `authentication_reference` is deliberately excluded because the pinned prompt redacts secret references.
- A 32-train/16-validation pilot has completed as documented below. No full v5 training, valid matched v5 behavior comparison, human review, or OpenAI v5 API evaluation has completed. `build-status.json` remains `complete_pending_human`. The final split remains procedurally sealed under ignored `training-runs/v5-sealed/v5-20260919-r1/`.
- The corrected GitHub notebook connected to a fresh Tesla T4 on 2026-09-21 and successfully mounted `/content/drive` under the signed-in account. The inventory reported 15,637,086,208 VRAM bytes and 66,553,233,408 bytes free on the mounted filesystem. `training/v5-colab-inventory-20260921.json` records that pre-install snapshot; the later pilot stages are recorded below.

## Implementation finding fixed during this audit

The Colab preflight cell called `verify-corpus-v5.py --corpus`, but the committed CLI required `--output` and `--sealed-output`. A fresh Colab clone cannot contain the ignored sealed final directory by design, so the cell would fail before the pilot.

The verifier now supports two explicit levels:

- `--output <public corpus>` verifies the manifest, every tracked public file, development scenarios/SFT reconstruction, split coverage, smoke membership, and full-input deduplication. It reports that sealed hashes were not verified.
- Adding `--sealed-output <local sealed root>` retains the stronger local byte-hash check without parsing labels.

The notebook now uses the public-only command, a regression test asserts the exact CLI, the suite passes 156 tests, and both public-only and full local verification pass.

## Colab token preflight finding fixed during the pilot attempt

The approved Colab setup pinned repository commit `7c2e8f24c978e9fff863d1995a8a2345451fea21`, installed the required packages, and verified the 4,548,221,488-byte Qwen3.5-2B weight file at publisher SHA-256 `aa33250c4fc64891ddfaba3a314fd9542ea371843c387178b425fbcc5ed680b1`. Before training, the length preflight incorrectly reported every example as two tokens and derived `max_length=128`.

Transformers 5.16.1 returned a mapping with `input_ids` and another field from `apply_chat_template`; the notebook measured the mapping's number of keys. The training helper already handled mapping and batched-list results correctly, but the duplicated notebook calculation did not. Training was stopped before the pilot cell, the notebook now unwraps `input_ids` and a possible batch dimension, and a regression test covers the required mapping path. Because the bad preflight was written to the original immutable run directory, the corrected notebook uses the new unique run directory `qwen35-2b-lora-v5-20260921T161000Z`. The suite passes 157 tests.

## Pilot result and inference-loader defect

The corrected run preflight measured 4,435 training examples at 198–3,529 tokens and 948 validation examples at 198–3,460 tokens, with means of 2,051.24 and 2,051.01. It selected `max_length=3584` without truncation. The corpus-manifest SHA-256 is `7e3c6f83bd39a73a386dbbf5e2a7932c45b68af46eaf6641e56474f2cc9dd021`.

The one-epoch T4 pilot completed in 541.02 seconds. It used 32 train and 16 validation examples and performed two optimizer steps. Training runtime was 436.7 seconds with loss 0.1738. Evaluation loss was 0.1218, mean token accuracy 0.9657, and evaluation runtime 53.03 seconds. These loss figures only establish that the training path ran; they are not task-quality evidence.

The saved loss-best adapter is 67,332,688 bytes with SHA-256 `603d322fb46f66f613e8a3e1b5e18c5ee53f7a16b2730dda39131f6e24726249`. Its run manifest SHA-256 is `98a367e684fd70d729c80fa8f90817c8f84109c9adfde5d7f75a712c97a9149f`, evaluation JSON SHA-256 is `25d3384c7905b66d4cd229e99f6fcfc7b427c775cd6bfe83034d022c2a0259da`, and checkpoint-2 completion marker SHA-256 is `649034b03907c4c64d7765dc8535fd30aaeaa89c65e3eb52653f3b50d511c523`.

The first development smoke attempt was interrupted after 512.6 seconds and left a preserved, invalid partial report plus eight call-journal records. A one-scenario diagnostic then completed for base and nominal LoRA. Both produced non-intent F1 0.0, provider success 1/4, raw JSON validity 3/4, raw schema validity 1/4, and identical raw-output SHA-256 `a199de7005174c8fad5b5ab6d8010e8c86df8375b46be3cdb236eca599e1da0e`.

This equality was caused by an inference defect. Training used `Qwen3_5ForConditionalGeneration` and saved 372 LoRA tensors under `base_model.model.model.language_model.layers...`. Inference forced `AutoModelForCausalLM`, loaded the 320-tensor text model under `base_model.model.model.layers...`, and PEFT warned that every adapter key was missing. A direct load with the recorded `Qwen3_5ForConditionalGeneration` class loaded all 372 LoRA tensors, all with nonzero magnitude, without a missing-key warning; text-only generation also executed.

The provider now reads the training model class from PEFT metadata, verifies it against the pinned base config architecture, uses that same architecture for both base and LoRA evaluation, and treats any PEFT missing-adapter-key warning as a hard failure. The full offline suite passes 161 tests. The original diagnostic reports remain preserved as invalid evidence and must not be used for model comparison.

## Data review and leakage interpretation

The 638-scenario review queue comprises 346 train, 61 validation, and all 231 final scenarios. Its reasons are 462 context-dependent/uncertain examples, 231 final-review requirements, and 60 stratified train samples; reasons can overlap.

The 6,663 cross-split overlap flags break down as:

| Flag class | Count |
| --- | ---: |
| Structural-only template similarity | 5,037 |
| Text Jaccard 0.72–0.8999 | 1,050 |
| Text Jaccard 0.90–0.9999 | 4 |
| Text Jaccard 1.0 on the representative utterance | 572 |

The generator reuses the same index-controlled facts and correction wording across domains, so some train/validation representatives differ only through prior context or domain facts. Full prompt+state input deduplication still reports zero duplicates, and the final split uses the held-out `heldout_handover` template family with the entire synthetic-health domain held out. This is a useful controlled synthetic benchmark, but it does not demonstrate linguistic independence or real-world generalization.

Reviewers should not click through 5,037 identical structural-family warnings individually. Before full training, they should:

1. Review every validation scenario and all 231 final scenarios for label semantics, naturalness, evidence, state transitions, and question relevance; the final reviewer must remain independent from model selection.
2. Review the 346 queued training scenarios and a cluster-level sample of overlap flags, grouping identical `(category, slot-set, template family)` patterns. Record cluster decisions and the number of covered pairs.
3. Reject recurring defects at the template/category level. If any recurring defect or unacceptable train/validation lexical cloning is found, create immutable `v5-...-r2`; do not edit r1 or its sealed final files.
4. Describe r1 as synthetic/template-generated and report held-out-domain/template subgroups separately. Do not claim zero leakage.

An actual human must sign the review artifact. Automated validation or this Codex audit is not human approval.

## Decision and next actions

1. Rerun the one-scenario base and pilot-LoRA diagnostic with the corrected, pinned provider. Confirm zero missing adapter keys, identical model architecture, and different raw-output hashes before scheduling a larger development smoke run.
2. If that gate passes, run a fresh matched development smoke comparison to measure failures, raw JSON validity, non-intent F1, correction behavior, latency, and token use. Preserve the old reports under their existing names.
3. Complete and hash the human-review ledger. Use a new corpus revision if review changes any frozen case or split.
4. Only after review approval and a credible pilot behavior result, run the fixed two-epoch 2B LoRA training in a new unique Drive directory. Evaluate the base and every retained epoch adapter on identical v5 validation cases under the same Colab runtime; select using non-intent exact F1 and the predeclared safety/JSON gates.
5. Run paid OpenAI validation only after an explicit cost decision. Freeze candidate settings and hashes, then perform the once-only final evaluation. Never inspect final cases to debug or retune.

Do not merge this branch to `main` until a corrected matched pilot behavior comparison passes and the review protocol is either completed or explicitly documented as pending research work.
